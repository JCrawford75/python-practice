import re
# Early practice with regex and file handling, lots of commented out parts as I struggled to figure out where things went sideways.
	# Open file
	#f = open('regexData2.txt', 'r')
	# Feed the file text into findall(); it returns a list of all the found strings


	#count = 0

	#pattern = re.compile(r'\d{3}\s\d{2}\s\d{4}')

	#matches = pattern.finditer(f)

	#for match in matches:
	#    print(match)
	# Open file
	f = open('regexData2.txt', 'r')
	# Feed the file text into findall(); it returns a list of all the found strings
	strings = re.findall(r'\d{3}\s\d{2}\s\d{4}', f.read())

	print(strings)

	f.close()
