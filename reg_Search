import re

# Improved and cleaned up some.

# Open file
f = open('regexData2.txt', 'r')
# Feed the file text into findall(); it returns a list of all the found strings
strings = re.findall(r'\d{3}\s\d{2}\s\d{4}', f.read())

print(strings)

f.close()
